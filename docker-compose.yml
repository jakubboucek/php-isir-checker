services:
    web:
        image: jakubboucek/lamp-devstack-php:8.3-cli
        environment:
            TZ: Europe/Prague
        restart: always
        volumes:
            - ".:/app"
        working_dir: /app
