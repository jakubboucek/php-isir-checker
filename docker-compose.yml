services:
    web:
        image: jakubboucek/lamp-devstack-php:7.3-legacy-cli
        environment:
            TZ: Europe/Prague
        restart: always
        volumes:
            - ".:/app"
        working_dir: /app
