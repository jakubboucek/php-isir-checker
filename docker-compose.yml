services:
    web:
        image: jakubboucek/lamp-devstack-php:8.2-cli
        environment:
            TZ: Europe/Prague
        restart: always
        volumes:
            - ".:/app"
        working_dir: /app
